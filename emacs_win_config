(custom-set-variables
 ;; custom-set-variables was added by Custom.
 ;; If you edit it by hand, you could mess it up, so be careful.
 ;; Your init file should contain only one such instance.
 ;; If there is more than one, they won't work right.
 '(ansi-color-names-vector
   ["#242424" "#e5786d" "#95e454" "#cae682" "#8ac6f2" "#333366" "#ccaa8f" "#f6f3e8"])
 '(cursor-type t)
 '(custom-enabled-themes (quote (wheatgrass)))
 '(markdown-command "pandoc"))

(custom-set-faces
 ;; custom-set-faces was added by Custom.
 ;; If you edit it by hand, you could mess it up, so be careful.
 ;; Your init file should contain only one such instance.
 ;; If there is more than one, they won't work right.
 '(highlight-indentation-face ((t (:inherit fringe :background "white smoke" :foreground "white")))))

;; Comment out/in region, maybe?

(defun comment-or-uncomment-line-or-region ()
  "Comments or uncomments the current line or region."
  (interactive)
  (if (region-active-p)
      (comment-or-uncomment-region (region-beginning) (region-end))
    (comment-or-uncomment-region (line-beginning-position) (line-end-position))
    )
  )

(global-set-key (kbd "C-;") 'comment-or-uncomment-line-or-region)

;; Changes yes or no to y or n
(defalias 'yes-or-no-p 'y-or-n-p)
;; Show parentheses
(show-paren-mode t)

(require 'package)
(add-to-list 'package-archives
             '("marmalade" . "http://marmalade-repo.org/packages/"))
(add-to-list 'package-archives
             '("melpa" . "http://melpa.org/packages/") t)

(package-initialize)

;; Interactively Do Everything
(setq ido-enable-flex-matching t)
(setq ido-everywhere t)
(ido-mode 1)

;; Dired mode settings
(add-hook 'dired-load-hook
	  (lambda ()
	    (load "dired-x")
	    ;; Set dired-x global variables here.  For example:
            ;; (setq dired-guess-shell-gnutar "gtar")
            ;; (setq dired-x-hands-off-my-keys nil)
	    ))
(add-hook 'dired-mode-hook
          (lambda ()
            ;; Set dired-x buffer-local variables here.  For example:
            ;; (dired-omit-mode 1)
            ))

;; Python debuger
(setq gud-pdb-command-name "python -m pdb")
;; Removing tool bar mode
(tool-bar-mode -1)

;; Tramp once again
(require 'tramp)
(add-to-list 'load-path "C:/Users/clizarraga/AppData/Roaming/.emacs.d/tramp/lisp")
;; (setq tramp-default-method "plink"
;;       tramp-default-user "clizarraga"
;;       tramp-default-host "apollo.ddpsc.org")
(setq tramp-default-method "plink"
      tramp-default-user "calizarr"
      tramp-default-host "MovingMoo"
      )

;; Python
(setq python-shell-interpreter "C:\Python27\python.exe")

;; ESS R settings
(setq ess-default-style 'Rstudio)

;; Remove shell command echo
(defun my-comint-init ()
  (setq comint-process-echoes t))
(add-hook 'comint-mode-hool 'my-commint-init)

;; Testing Dired Titles
(setq frame-title-format
      (list (format "%s %%S: %%j " (system-name))
	    '(buffer-file-name "%f" (dired-directory dired-directory "%b"))))

;; No more bullshit hiding
(global-unset-key (kbd "C-z"))

;; Ansi-Color mode
(add-hook 'shell-mode-hook
	  'ansi-color-for-comint-mode-on)
