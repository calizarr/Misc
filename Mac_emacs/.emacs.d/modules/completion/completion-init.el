;; Load Company Configuration
(load "company-conf.el")
;; Load Ido Configuration
;; (load "ido-conf.el")
;; Load Helm Ido Like Configuration
;; (load "helm-ido-like-conf.el")
;; Add in helm settings
(load "helm-conf.el")
